name: Build

on:
  push:
    branches: [master, main, develop]
  pull_request:
    branches: [master, main, develop]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      # Pushes and manual runs retain full suites. PRs compare against the
      # immutable target SHA, including when checkout uses a merge commit.
      WODE_TEST_BASE: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || '' }}
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 12.4.2
          run_install: false
      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24.11.1
          cache: pnpm

      - name: Install Buf CLI
        uses: bufbuild/buf-setup-action@v1

      - name: Install just
        uses: taiki-e/install-action@v2
        with:
          tool: just@1.38.0

      - name: Verify workspace
        run: just ci

      - name: Install Storybook browser
        run: pnpm exec playwright install --with-deps chromium

      - name: Verify components registry and Storybook
        run: pnpm -C apps/components verify

      - name: Upload components Pages artifact
        if: github.repository == 'wenerme/wode' && github.event_name == 'push' && github.ref_name == github.event.repository.default_branch
        uses: actions/upload-pages-artifact@v3
        with:
          path: apps/components/storybook-static

  pages:
    if: github.repository == 'wenerme/wode' && github.event_name == 'push' && github.ref_name == github.event.repository.default_branch
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    concurrency:
      group: pages
      cancel-in-progress: false
    steps:
      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Deploy components Pages
        id: deployment
        uses: actions/deploy-pages@v4
